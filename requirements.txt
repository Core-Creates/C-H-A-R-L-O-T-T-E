# CHARLOTTE - Dependency List
# Compatible with Python 3.11

# CLI input handling
InquirerPy>=0.3.4

# CVE report formatting
markdown2>=2.4.10
reportlab>=4.0.4

# HTTP and API requests
requests>=2.32.0

# YAML file handling
PyYAML>=6.0.1

# Vulnerability dataset access (Hugging Face)
datasets>=2.18.0

lxml>=4.9.3
# Optional: If using embeddings / transformers later
# sentence-transformers>=2.5.1
# huggingface-hub>=0.24.0
# Optional: If using OpenAI API for embeddings
# openai>=0.27.8

# Optional: If using Nmap for scanning
# python-nmap>=0.7.1 
python-nmap

# Optional: If using Scapy for network packet manipulation
scapy>=2.5.0


# Optional: If using Pandas for data manipulation
pandas>=2.1.3

# Optional: If using Matplotlib for plotting
matplotlib>=3.8.0

# Optional: If using Jinja2 for templating
jinja2>=3.1.4

# Optional: If using Flask for web interface
flask>=2.3.3

# Optional: If using Celery for task queues
celery>=5.3.0
# Optional: If using Redis for message brokering and caching
# Note: Ensure Redis server is running if using this
redis>=4.6.0
# Optional: If using BeautifulSoup for HTML parsing
beautifulsoup4>=4.12.2
# Optional: If using SQLAlchemy for ORM
sqlalchemy>=2.0.20

# Optional: If using Pytest for testing
pytest>=8.3.2

tabulate

# Optional: If using Click for command line interfaces
click>=8.1.7
# Optional: If using PyInstaller for packaging
# pyinstaller>=5.12.0

scikit-learn>=1.4.0

tqdm

pymetasploit3

torch